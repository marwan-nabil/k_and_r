#include <stdio.h>
#include <token.h>

int main()
{
  printf("\nThis is a simple reverse polish calculator.\n"
	 "Features:\n-single character (alphabetic) variables\n-trigonometric functions "
	 "sin(x), cos(x).\nUsage: input is tokenized on white spaces,\n"
	 "maximum token length allowed is %d", MAX_TOKEN-2);
  
}

